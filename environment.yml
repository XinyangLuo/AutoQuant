name: AutoQuant
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11.15
  - python=3.11.15
  - pandas
  - pyarrow
  - numpy
  - duckdb
  - matplotlib
  - tqdm
  - pytest
  - pip
  - pip:
      - tushare>=1.3
      - openai>=1.30
      - mcp-pdf
      - python-dotenv
      - pyyaml
